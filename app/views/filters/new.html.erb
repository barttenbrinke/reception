<h1>New filter for <%=h @source.to_s %></h1>

<% form_for(@filter, :url => source_filters_path(@source)) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label "The word" %><br />
    <%= f.text_field :keyword %>
  </p>
  <p>
    <% if @filter.positive %>
      has to be present for the torrent to download.
    <% else %>
      should <b>NOT</b> be present for a torrent to download.  
    <% end %>
    <%= f.hidden_field :positive %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back to source', source_path(@source) %>